# 第4章 数据处理与变量构建写作 Skill

## 适用场景

用于撰写博士论文的数据来源、样本处理、变量构建、描述性统计和事实特征章节。

## 核心原则

- 数据口径必须清楚，不能用模糊表达替代变量定义。
- 不得编造数据来源、样本量、均值、标准差或图表结果。
- 如果缺少真实数据结果，应使用“待根据数据结果填写”。

## 章节应包含内容

### 4.1 数据来源与样本范围

应说明：

- 企业数据来源，如工商注册数据、工业企业数据库、上市公司数据、税收数据、招聘数据等；
- 城市层面数据来源，如统计年鉴、城市统计年鉴、人口普查、夜间灯光、土地、交通、财政等数据；
- 样本时间范围；
- 空间范围和城市层级；
- 企业与城市数据匹配规则；
- 样本筛选规则。

### 4.2 数据清洗与处理流程

应包括：

- 重复值处理；
- 缺失值处理；
- 异常值识别和缩尾规则；
- 行政区划调整与城市代码统一；
- 价格变量平减；
- 地理坐标、距离或可达性变量处理；
- 企业行业分类、所有制分类和规模分类规则；
- 最终样本形成过程。

### 4.3 变量定义与构建

变量表至少包括：

- 被解释变量：城市规模分布或城市规模相关指标；
- 核心解释变量：企业选址、企业进入、企业集聚或区位选择指标；
- 控制变量：经济发展、人口、工资、土地、财政、公共服务、交通、产业结构等；
- 机制变量：市场潜力、集聚经济、交通可达性、制度环境等；
- 异质性分组变量：区域、城市等级、行业类型、企业类型等。

每个变量应说明：

- 变量名称；
- 符号；
- 计算方法；
- 单位；
- 数据来源；
- 预期影响方向。

### 4.4 描述性统计

应包括：

- 样本数量；
- 均值、标准差、最小值、最大值；
- 关键变量分布特征；
- 异常值和离散程度解释；
- 与后续模型设定的关系。

### 4.5 事实描述与空间格局

可包括：

- 企业选址的空间分布；
- 城市规模分布演化；
- 不同区域、城市等级和行业的差异；
- 城市位序—规模关系或 Zipf 指数描述；
- 企业进入与城市规模变化的初步相关性；
- 地图、趋势图、散点图或核密度图的文字解释。

## 输出格式

优先输出以下内容之一：

- 数据章节完整初稿；
- 变量定义表；
- 数据处理流程表；
- 描述性统计解读文字；
- 图表结果解读模板；
- 数据口径问题清单。

## 写作注意事项

- 所有数据处理决定都要能解释其合理性；
- 描述性统计只说明事实和相关性，不做因果推断；
- 若图表未提供，不得虚构图表中的数值和趋势；
- 与后文实证变量名称保持一致。
