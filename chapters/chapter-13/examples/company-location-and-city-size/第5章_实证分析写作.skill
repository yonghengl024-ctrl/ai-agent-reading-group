# 第5章 实证分析写作 Skill

## 适用场景

用于撰写博士论文基准实证模型、识别策略、回归结果和经济含义解释。

## 核心原则

- 不得编造回归系数、显著性水平、样本量、R² 或固定效应设置。
- 若用户未提供结果表，应输出“结果解读模板”而非虚假结果。
- 区分相关性和因果关系；只有在识别策略充分时才使用因果措辞。

## 章节应包含内容

### 5.1 计量模型设定

应说明：

- 被解释变量；
- 核心解释变量；
- 控制变量；
- 城市固定效应、年份固定效应、行业固定效应或其他固定效应；
- 标准误聚类层级；
- 样本范围；
- 模型形式，如面板固定效应模型、空间计量模型、离散选择模型、DID、IV 等。

基本表达模板：

\[
Y_{ct}=\alpha+\beta FirmLocation_{ct}+\gamma X_{ct}+\mu_c+\lambda_t+\varepsilon_{ct}
\]

其中，需解释每个符号含义，并说明 \(\beta\) 的经济含义。

### 5.2 识别策略说明

应讨论：

- 核心识别假设；
- 可能的内生性来源，如反向因果、遗漏变量、选择偏误、测量误差；
- 固定效应和控制变量如何缓解偏误；
- 若有政策冲击、工具变量或准自然实验，应说明其有效性。

### 5.3 基准回归结果

解读回归表时应包括：

- 表格列设置；
- 控制变量和固定效应逐步加入过程；
- 核心系数方向、大小和显著性；
- 经济意义解释，而不只是统计显著性；
- 与理论假设的对应关系。

### 5.4 结果解释与讨论

应回答：

- 企业选址变量如何影响城市规模分布；
- 结果是否支持理论假设；
- 结果与现有文献是否一致；
- 在中国城市体系或区域发展背景下如何理解；
- 是否存在需要后续稳健性和机制检验进一步验证的问题。

## 回归结果解读模板

当用户提供表格后，按以下结构写作：

1. “表 X 报告了……的基准回归结果。”
2. “第（1）列仅控制……，第（2）列进一步加入……，第（3）列控制……固定效应。”
3. “核心解释变量的系数为……，在……水平上显著，表明……”
4. “从经济意义看，……每提高 1 个单位/1 个标准差，……变化……”
5. “上述结果与假设 H… 一致/不一致，说明……”
6. “不过，基准结果仍可能受到……影响，后文将通过……进行检验。”

## 输出格式

- 计量模型文字；
- 识别策略说明；
- 回归表解读；
- 实证章节完整初稿；
- 根据审稿/导师意见修改后的版本。
