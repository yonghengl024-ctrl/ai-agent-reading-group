# 第二章“章节大纲生成”Skill 触发记录

测试对象：第二章 文献综述

测试目的：检查用户使用不同自然语言表述时，是否应识别为“章节大纲生成”意图，并调用/适用章节大纲生成规则。

判定标准：

- 表述中包含“大纲、结构、框架、小节、规划、重构”等意图词；
- 指向“这一章、当前主题章节、当前章节”等章节级对象；
- 目标是生成或重组章节内容结构，而非直接撰写完整正文；
- 对第二章而言，应结合 `大纲生成.skill` 与 `第2章_文献综述写作.skill` 的规则生成文献综述章节大纲。

| 序号 | 用户表述 | 是否正确触发“章节大纲生成”Skill | 判断理由 | 推荐响应动作 |
|---:|---|---|---|---|
| 1 | 帮我写这一章的大纲 | 是 | 明确包含“这一章”和“大纲”，是直接的章节大纲生成请求。 | 生成第二章文献综述章节大纲。 |
| 2 | 生成当前主题章节的结构 | 是 | “章节的结构”与章节大纲生成等价，且对象为当前主题章节。 | 生成第二章结构化章节框架。 |
| 3 | 列出这一章应该有哪些小节 | 是 | “列出小节”属于大纲生成的核心任务，要求确定章节内部结构。 | 输出第二章小节清单及各小节功能。 |
| 4 | 请规划当前章节的内容框架 | 是 | “规划”“内容框架”表示章节级结构设计，应触发大纲生成。 | 生成章节框架、逻辑链条和写作重点。 |
| 5 | 帮我做一个章节大纲 | 是 | “章节大纲”是明确触发词。 | 生成第二章章节大纲。 |
| 6 | 重构这一章 | 是 | 虽未出现“大纲”，但“重构这一章”通常表示重新组织章节结构，应触发章节大纲/结构重组；若已有正文，则应先读取原文再重构。 | 若无现有正文，生成重构后的第二章大纲；若有正文，先读取后重构。 |

## 结论

以上 6 种表述均应正确触发“章节大纲生成”Skill。对于第二章，实际执行时应以 `第2章_文献综述写作.skill` 为章节内容约束，并参考 `大纲生成.skill` 的结构化输出要求。

## 本次已生成的大纲文件

- `02_文献综述/outline.qmd`

该文件包含第二章文献综述的章节大纲、小节安排、每节功能说明、章节逻辑和待补充材料。
